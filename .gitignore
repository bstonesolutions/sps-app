# dependencies
node_modules/

# build output
dist/

# generated by `cap sync` (contains the built web bundle incl. the maps key) — regenerated locally
ios/App/App/public/

# local env files — SECRETS, never commit (Vite reads these at build time)
.env
.env.local
.env.*.local

# macOS
.DS_Store
**/.DS_Store

# local tooling
.claude/
